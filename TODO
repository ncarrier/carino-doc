current tasks being worked on :
 * make a console client for controlling the vehicles
 * create a webapp for controlling the vehicles
 * identify the perimeter of a minimal protocol library for clients
   implementation and implement it in libci
 * make the android application rely on libci

BUGS
 * multiple reconnections with infocus M2

development :

integration :

source distribution, versionning and backup :
 * create a bootstrap script, for bootstrapping the environment for hacking the
   code, the schematics and the mechanics
 * setup an (at least minimal) continuous integration

documentation:
 * create an FAQ
 * create the website and generate HTML web pages to populate it from markdown
 * create and populate a base wiki on the project (on github ?, how to manage
   french translation ?)

build system :
 * remove all the direct usages of ${TOOLCHAIN_PREFIX} in build scripts if
   possible
 * separate vehicles
 * allow a pclinux build
 * separate installation related configuration from the rest
 * make bau create directories prior to pushing
 * prevent bau from recreating existing symlinks
 * study already existing build systems
 * avoid reconfiguring a package when it has already been built
 * dependency handling, even minimalist

platform oss initiatives study :
 * dronecode
   * APM/ArduPilot
   * Pixhawk
 * paparazzi uav
 * ROS
 * yarp
